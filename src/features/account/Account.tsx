import React from 'react';
import s from './Account.module.scss'

type accountType = {};

export const Account = (props: accountType) => {
   return (
       <div>
          Account
       </div>
   );
};