import React from 'react';
import s from './Messages.module.scss'

type messagesType = {};

export const Messages = (props: messagesType) => {
   return (
       <div>
          Messages
       </div>
   );
};