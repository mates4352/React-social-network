import React from 'react';
import s from './Settings.module.scss'

type settingsType = {};

export const Settings = (props: settingsType) => {
   return (
       <div>
          Settings
       </div>
   );
};