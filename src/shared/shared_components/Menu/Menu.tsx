import s from './Menu.module.scss'

export const Menu = () => {
   return (
       <nav className={s.menu}>
          <ul className={s.list}>
             <li className={s.item}>
             </li>
          </ul>
       </nav>
   );
}